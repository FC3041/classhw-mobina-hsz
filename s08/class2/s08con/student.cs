public class Student{
    string name;
    int id;
    public Student(string fname, int nid){
        
    }

    // We call the static ObjectS on student and we call the objects on varuable objects of student
}